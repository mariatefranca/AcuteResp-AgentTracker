# This is a Databricks asset bundle definition for acute_resp_agent_job.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
bundle:
  name: acute_resp_agent
  uuid: 45f57fa5-deef-422b-a652-51962187ce96

# artifacts:
#   python_artifact:
#     type: whl
#     build: uv build --wheel

include:
  - resources/*.yml

targets:
  dev:
    # The default target uses 'mode: development' to create a development copy.
    # - Deployed resources get prefixed with '[dev my_user_name]'
    # - Any job schedules and triggers are paused by default.
    # See also https://docs.databricks.com/dev-tools/bundles/deployment-modes.html.
    mode: development
    default: true
    workspace:
      host: https://dbc-1b9099b8-01a8.cloud.databricks.com

  prod:
    mode: production
    workspace:
      host: https://dbc-1b9099b8-01a8.cloud.databricks.com
      # We explicitly deploy to /Workspace/Users/mt3franca@gmail.com to make sure we only have a single copy.
      root_path: /Workspace/Users/${workspace.current_user.userName}/.bundle/${bundle.name}/${bundle.target}
    permissions:
      - user_name: ${workspace.current_user.userName}
        level: CAN_MANAGE
